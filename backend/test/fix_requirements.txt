# --- CORE MATEMATICO (Vincolato) ---
numpy==1.26.4
protobuf==3.20.2

# --- PADDLE STACK (Priorità 1) ---
paddlepaddle==2.6.2
paddleocr>=2.7

# --- TORCH CPU STACK (Priorità 2 - Link Esplicito) ---
# Usiamo la sintassi --extra-index-url per dire a pip dove trovare la CPU build
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu

# --- VISION & NLP (Alto Livello) ---
ultralytics>=8.3.0
gliner>=0.2.0
opencv-python<=4.10.0.84