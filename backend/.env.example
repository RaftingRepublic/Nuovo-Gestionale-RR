# backend/.env.example
# Copia questo file come .env e configura i valori

# Configurazione Base
APP_ENV=production
LOG_LEVEL=INFO

# Email (SMTP per invio waiver/registrazioni)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=none
SMTP_PASS=none
SMTP_FROM=noreply@raftingrepublic.com

# --- COMPLIANCE LEGALE & PRIVACY ---
# NONE: Le immagini vengono elaborate in RAM e distrutte subito (GDPR Compliant)
# DEBUG: Le immagini vengono salvate in storage/dataset_raw (Solo per sviluppo)
ID_IMAGE_RETENTION=NONE

# --- AZURE AI (Document Intelligence per OCR documenti d'identità) ---
AZURE_DOCUMENT_ENDPOINT=https://YOUR-RESOURCE.cognitiveservices.azure.com/
AZURE_DOCUMENT_KEY=your-azure-key-here
